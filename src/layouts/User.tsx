import UserList from "../components/UserList";
import UserPosts from '../components/UserPosts'

const User = () => {
  return (
    <div>
      <UserList />
      <UserPosts/>
    </div>
  );
};

export default User;
